Todo:
	Build a window with a scrollbar (maybe as a subclass even)
		-> getChildrenDimension ("Think about")
	Image-functionality in backend
	Cursor-support in backend
	Mutliple font support

Widgets:
	x Windows
		x Window-Resizing + Event (only diagonal)
		x Window-Closing + Event
	x Label
	x Buttons
	x Checkbox
	x FormLayout / LineLayout
	x Category
	x Radio Buttons
	x Number Wheel
	x Line
	x Input Field
		scrolling, when line is too long for text edit
	x Number wheel durch Input Box ergänzen
	x Scroll bars (vertical, horizontal)
	x Treeview

	Maybe:
		Context menus
		Menu bars
		Toolbars
		Slider

Think about:
	For showing/hiding scrollbars widgets should be able to determine a bounding box of their children

	Some things have to be handled in theme.update, but they shouldn't maybe have the possibility for setters to be called for themes?

	Should vertical and horizontal scrollbars be different classes? (I think yes, but I'm too lazy)

	Do 'on' callbacks really have to be set first? They are not even called by Base.init? (was I stupid?)

Bugs:
	Numberwheel Text Input is all kinds of strange (I don't even know what exactly goes wrong)
	Sometimes the program crashes, saying: 'UTF-8 decoding error: not enough space'. This happens when you try to delete ö,ä,ü
		https://love2d.org/wiki/utf8
		https://love2d.org/wiki/love.textinput
		https://love2d.org/forums/viewtopic.php?f=4&t=8760

Für Doku merken:
	gui.widgets.setDefault("theme", "dark") um theme zu setzen
	setParam verwenden, wenn möglich oder am Anfang übergeben
	widget.enabled nicht vergessen
	Bei der Parameterliste am Anfang kann man nie wissen in welcher Reihenfolge die gesetzt werden (wegen pairs()).
	Man darf also nur Dinger setzen, die unabhängig voneinander sind. Ansonsten braucht man mehrere calls zu setParam.

----------- Wirre Notizen:
	pass
