Todo:
	Think about scissorStack[1]. Can this be done without ridiculous constants? Otherwise comment this.
	Fix window dragging. Somehow make it special?
	Fix onMouseLeave (so it will be called, when another widgets takes the 'hovership')
	focus every widget that claimes a mousePressed

Widgets:
	x Windows
		x Window-Resizing + Event
			Nur Diagonal! - vllt. mit kleiner Box unten rechts?
		x Window-Closing + Event
	x Label
	x Buttons
	x Checkbox
	x FormLayout / LineLayout
	x Category
	x Radio Buttons
	Number Wheel
	Treeview
	Input Field
	Number Box
		Input Field + 2 Buttons
	Slider (vielleicht nicht)

Nachdenken:
	Drop-Downs?...Oder Number-Wheels?
		-> Wie kann man Widgets bauen, die auf allem gezeichnet werden und nicht von einem Canvas beschränkt werden?
		Es ist einfach widgets widgets ein "breakout"-Attribut zu geben, dass an pushCanvas weitergegeben wird und dann beim
		scissor-canvas den 0, 0, 100000, 100000 stattdessen pusht, aber es bleibt das Problem der Renderreihenfolge bestehen.
		Fensterränder werden weiterhin über diesen Widgets gezeichnet.

	onResize für alle Widgets? Viele müssen Sachen tun, wenn die resized werden
		Kinder resizen

Bugs:
	pass

Für Doku merken:
	gui.widgets.setDefault("theme", "dark") um theme zu setzen
	setParam verwenden, wenn möglich oder am Anfang übergeben
	widget.enabled nicht vergessen
	Bei der Parameterliste am Anfang kann man nie wissen in welcher Reihenfolge die gesetzt werden (wegen pairs()).
	Man darf also nur Dinger setzen, die unabhängig voneinander sind. Ansonsten braucht man mehrere calls zu setParam.

----------- Wirre Notizen:
	pass
