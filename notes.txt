Widgets:
	x Windows
		x Window-Resizing + Event
			Nur Diagonal! - vllt. mit kleiner Box unten rechts?
		x Window-Closing + Event
	x Label
	x Buttons
	x Checkbox
	x FormLayout / LineLayout
	Category
	Treeview
	Radio Buttons
	Input Field
	Number Box
	Slider

Nachdenken:
	Drop-Downs?...Oder Number-Wheels?
		-> Wie kann man Widgets bauen, die auf allem gezeichnet werden und nicht von einem Canvas beschränkt werden?
		Es ist einfach widgets widgets ein "breakout"-Attribut zu geben, dass an pushCanvas weitergegeben wird und dann beim scissor-canvas den 0, 0, 100000, 100000 stattdessen pusht, aber es bleibt das Problem der Renderreihenfolge bestehen. Fensterränder werden weiterhin über diesen Widgets gezeichnet.
			
Bugs:
	
	
Für Doku merken:
gui.widgets.setDefault("theme", "dark") um theme zu setzen
setParam verwenden, wenn möglich oder am Anfang übergeben
widget.enabled nicht vergessen

----------- Wirre Notizen:

-- all categories in properties-window:
function propCategoryCollapse(category, collapsed)
	if not collapsed then
		for child in ipairs(category.parent.children) do
			if child.type == "Category" then
				child:collapse(true)
			end
		end
	end
end