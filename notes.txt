Todo:
	Fenster mit Scrollbar bauen (vllt. als Subclass)
	Image-Funktionalität im Backend
	Cursor-Funktionalität

Widgets:
	x Windows
		x Window-Resizing + Event
			Nur Diagonal! - vllt. mit kleiner Box unten rechts?
		x Window-Closing + Event
	x Label
	x Buttons
	x Checkbox
	x FormLayout / LineLayout
	x Category
	x Radio Buttons
	x Number Wheel
	x Line
	x Input Field
		scrolling, when line is too long for text edit
	x Number wheel durch Input Box ergänzen

	x Scroll bars (vertical, horizontal)
	Treeview

	Vielleicht:
		Context menus
		Menu bars
		Toolbars
		Slider

Nachdenken:
	Layouter muss irgendwie Größe des Contents zurückgeben können, damit man scrollbars ein- und ausblenden kann.
		Vielleicht als separate Methode? Sodass man die aufrufen kann, bevor man arrange()ed
		Das könnte behindert sein, weil man auch content hat, der nicht gelayoutet ist. Man sollte stattdessen eine Bounding-Box für alle child-Elemente finden können.
			Man muss dann halt zwei mal layouten

	warum rufe ich beim closeButton im Window nicht einfach :draw auf? statt themeFunction.
	sollte der closeButton im Window in theme.init erstellt werden?

	scrollBarV und scrollBarH als verschiedene klassen?

Bugs:
	pass

Für Doku merken:
	gui.widgets.setDefault("theme", "dark") um theme zu setzen
	setParam verwenden, wenn möglich oder am Anfang übergeben
	widget.enabled nicht vergessen
	Bei der Parameterliste am Anfang kann man nie wissen in welcher Reihenfolge die gesetzt werden (wegen pairs()).
	Man darf also nur Dinger setzen, die unabhängig voneinander sind. Ansonsten braucht man mehrere calls zu setParam.

----------- Wirre Notizen:
	pass
