Widgets:
	x Windows
		x Window-Resizing + Event
			Nur Diagonal! - vllt. mit kleiner Box unten rechts?
		x Window-Closing + Event
	x Label
	x Buttons
	x Checkbox
	FormLayout / LineLayout
		:addLine(oneWidget), :addLine{widgets}
	Category
	Treeview
	Radio Buttons
	Input Field
	Number Box
	Slider

Nachdenken:
	Koordinatenursprung sollte in Windows unter der Window-Leiste sein
	withCanvas zum helper machen, der auch prüft ob self überhaupt gesetzt ist und dann withCanvas(childCanvas) machen, wobei childCanvas, wie ein widget position, width, height hat
		withCanvas(rectLike, func, ...)
	Alternativ kann man das einfach lassen und beim Layouter einfach top-paddings und so angeben
		
Bugs:
	
	
Für Doku merken:
gui.widgets.setDefault("theme", "dark") um theme zu setzen
setParam verwenden, wenn möglich oder am Anfang übergeben
widget.enabled nicht vergessen

----------- Wirre Notizen:

-- all categories in properties-window:
function propCategoryCollapse(category, collapsed)
	if not collapsed then
		for child in ipairs(category.parent.children) do
			if child.type == "Category" then
				child:collapse(true)
			end
		end
	end
end